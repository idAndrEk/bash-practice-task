# роверь, запущен ли скрипт от имени root: если да — выведи "Доступ разрешён", иначе — "Нужны права администратора".

#!/bin/bash
if [ "$EUID" -ne 0 ]; then
    echo "Требуется root" >&2
    exit 1
fi